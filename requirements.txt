# veri seti uretimi ve denetimi (Python 3.10+, ek bagimlilik yok)
# --- yukleme
huggingface_hub>=0.26
datasets>=3.0
# --- egitim (GPU)
transformers>=4.46
trl>=0.12
peft>=0.13
bitsandbytes>=0.44
accelerate>=1.0
torch>=2.4
# --- LLM-as-judge (opsiyonel)
anthropic>=0.40
